<h1>My tasks</h1>
<%= link_to 'Add a task', new_path, class:'btn btn-light m-3'%>
<% @tasks.each do |task| %>
<div class='d-flex align-items-center m-3'>
<%= task.completed %>
<%= task.title %>
<%= link_to 'Show task', task_path(task), class:'btn btn-primary'%>
</div>
<% end %>
